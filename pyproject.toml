[project]
name = "qt-threading-tutorial"
version = "0.1.0"
description = "qt threading is hard, this should help"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "pyqt6>=6.11.0",
]
